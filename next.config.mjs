/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['veberszone.com', 'pallavibobde.github.io', 'localhost','github.com','raw.githubusercontent.com'] // <== Domain name
  }
};

export default nextConfig;
