/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images : {
    domains : ['veberszone.com', 'pallavibobde.github.io', 'localhost'] // <== Domain name
  }
};

export default nextConfig;
