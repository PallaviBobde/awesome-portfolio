const Logo = ({}) => {
  return (
    <a href="/" className="text-white text-lg font-semibold menu-item">
      <span className="gradient-text text-6xl">P</span>
      <span className="ml-[-20px] font-normal">allavi</span>
    </a>
  );
};

export default Logo;
